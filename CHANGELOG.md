# Changelog

## v3.3.7 (2026-09-08)

### 修复
- 批注下拉卡内删除/编辑批注后列表不即时刷新：命令写回内容后主动同步 orca.state.blocks 快照，条目立即消失、计数即时更新（覆盖添加/编辑/删除/撤销）。

### 样式
- 顶栏「批注」按钮图标重新设计：自定义波浪线 + 角标圆点 SVG（Fluent 2 线性、跟随主题色），替代原 Tabler notes 图标，悬停提亮微缩放。

## v3.3.6 (2026-09-08)

### 变更
- 「全部文档」模式由后端全库扫描改为**缓存积累**：打开文档或增删改批注时，后台把该文档的批注索引写入插件数据（跨会话持久化，上限 500 文档自动淘汰最旧）；汇总时用内存块表实时结果合并已积累的缓存，不再依赖 query / get-aliases / get-block 等后端枚举 API，稳定可预期，首次可能只显示已访问文档、随使用逐渐覆盖全库。
- 批注下拉卡条目统一为瘦身形状（原文/批注/序号/块 id/预览），渲染与跳转不再依赖完整块对象。

### 优化
- 清理死代码：移除未使用的 getActiveRootBlockId / findViewPanel / 回收站 snapshotPage / 未用依赖与过时注释。
## v3.3.5 (2026-09-08)

### 修复
- 全部文档模式统计不全：根因是 orca.state.blocks 只含已加载到内存的块（非全库表），原先仅靠内存全扫只能统计到已打开文档。现改为「内存即时统计 + 后端补全」双通道：先用内存结果即时出数，再通过后端 query（顶层块）+ get-block-tree（深挖每页批注）补齐全库并合并去重。
- 全部文档模式下点击未加载文档的批注「删除」不再静默失败，提示先跳转打开该文档。
## v3.3.4 (2026-09-08)

### 样式
- 页签栏改为 macOS / Apple 风格：毛玻璃（frosted glass）页签条、圆润胶囊标签、激活标签白色毛玻璃漂浮凸起（含顶部玻璃高光与柔和投影）、hover 关闭按钮红色圆（traffic light 意象）、拖拽位置胶囊描边提示；垂直模式采用 Finder 侧栏式浅色胶囊选中态。默认字体栈加入 -apple-system / SF Pro。
## v3.3.3 (2026-09-08)

### 修复
- 批注卡草稿残留：切换/重新打开批注时不再串用上一次未保存的输入内容。
- 回收站恢复丢失富文本：快照补存块 content（含批注 fragment/加粗/链接等），恢复时原样还原，不再退化为纯文本。
- 回收站幽灵快照：删除拦截改为两阶段——先全部取回快照、全部成功才落盘；任一写入失败回滚已写文件并取消删除，杜绝“页面还在但回收站多出条目”。
- 恢复快照文件损坏/缺页时给出明确错误提示，不再静默抛错。
- 移除无效命令「查看当前块批注」（打开即关闭的死命令）及其未使用依赖。
- 顶栏「批注」按钮徽标与下拉卡计数统一（全部文档模式下显示全库总数，不再两处不一致）。

### 新增
- 生效快捷键 **Ctrl+Alt+A** 快捷添加批注（原为空状态提示但未实现）。

### 优化
- 全部文档模式下全库扫描改为 150ms 防抖，避免打字/编辑时每次快照变化触发全库遍历。
- 清理过时文件与文案：删除旧图标生成脚本 gen-icon.cjs（避免误覆盖新图标）、移除面板时代的死翻译、修正入口文件注释。
## v3.3.2 (2026-09-07)

### 新增
- 批注下拉卡头部恢复范围切换按钮（仅当前文档 ⇄ 全部文档）：切换通过 orca.plugins.setSettings 写入插件设置，与设置面板中的「批注下拉卡范围」双向同步、即时生效。
## v3.3.1 (2026-09-07)

### 变更
- 批注下拉卡显示范围改为插件设置项「批注下拉卡范围」（仅当前文档 / 全部文档，singleChoice），替代 v3.3.0 的头部切换按钮与 localStorage 记忆，设置即时生效。
## v3.3.0 (2026-09-07)

### 新增
- 批注下拉卡支持「全部文档」模式：头部新增范围切换按钮（仅当前文档 ⇄ 全部文档，localStorage 记忆），全局模式扫描全部页面并按页面标题分组展示，当前文档排最前、其余按标题排序，点击条目仍可跳转与删除。
## v3.2.4 (2026-09-07)

### 变更
- 重写插件介绍：README 与市场描述同步更新（批注改为顶栏下拉卡汇总，补充 Fluent 2 设计语言与跨设备同步说明；中英文文案统一）。
- 重绘插件图标：Fluent 2 几何风格，蓝渐变圆角底 + 批注气泡 / 页签条 / 回收站三功能符号。
## v3.2.3 (2026-09-07)

### 变更
- 激活页签描边改为强调色（蓝色，跟随 Orca 主题色/页签栏强调色设置），水平与垂直模式统一。
## v3.2.2 (2026-09-07)

### 变更
- 页签栏按 Fluent 2（Win11 资源管理器式）重构：页签条改不透明 bg-2 底色层，移除亚克力模糊；激活页签为 bg-1 卡片（细描边 + 微阴影），非聚焦面板当前页签为半强度表面。
- 固定/关闭按钮统一为 Fluent 20×20 圆角热区；垂直模式激活页签增加左侧 3px 强调条（Fluent v9 垂直页签规范）。
## v3.2.1 (2026-09-07)

### 修复
- 顶栏批注下拉卡改为不透明背景（移除亚克力模糊，补边框），避免正文透出影响阅读。
- 下拉卡内批注内容字号跟随 Orca 正文字号（与行内原文一致）。
## v3.2.0 (2026-09-07)

### 变更
- 移除批注汇总侧栏（pizhu.panel 面板与 openPanel 命令），改为顶栏「批注」按钮的 Popup 下拉卡：点击展开当前文档批注汇总，点击外部或 Esc 关闭，不占用任何分栏空间。
- 顶栏按钮内嵌当前文档批注计数徽标，悬停可见。
- 升级自动清理旧版本遗留的 pizhu.panel 侧栏面板。
## v3.1.3 (2026-09-07)

### 修复
- 批注侧栏打开后默认分割比例从 50:50 调整为约 75:25，只占窗口四分之一宽度。
- 批注汇总面板在 journal 日记视图下原本收集不到批注（journal 的根块 id 不在 viewState.rootBlockId 里），现通过 get-journal-block 后端 API 从 viewArgs.date 正确换取日期页根块 id；另加兜底逻辑：遍历 blocks 全表找含批注的块沿 parent 链回溯根块。
## v3.1.2 (2026-09-05)

### 修复
- 批注编辑卡的输入框移除自带滚动条与右下角拖拽手柄：高度由内容自动撑高，溢出由卡片滚动区统一处理，界面更干净。
## v3.1.1 (2026-09-05)

### 修复
- 回收站对 `orca.invokeBackend` 的拦截改为链式安全：卸载时仅在自己处于链顶才恢复原函数，不再覆盖其他插件后装的 hook；被外层插件包裹时也能立即停止拦截并透传。
- 批注卡在批注已被删除时于渲染期间直接修改全局状态（React 反模式），改为 useEffect 中安全关闭。
- 保存空批注内容时点击「保存」不再静默无响应，改为明确提示「批注内容不能为空」。
- 跨样式选区（首尾字号/颜色/粗斜体不一致）添加批注时不再强制套用起点样式，保持块级继承，避免后半段文字样式被错误覆盖。
- 添加批注输入浮层改用 DOM API 构建，移除 innerHTML 字符串拼接（原已转义，双保险）。
- 批注树收集增加环与深度保护，异常数据不再可能死循环。
- 页签栏启用时重复添加两次 body class 的小冗余清理。
## v3.1.0 (2026-09-05)

### 改进
- 整体 UI 重构为微软 Fluent 2 设计语言：Segoe UI Variable 字体、4/8/10px 圆角体系、亚克力（Acrylic）半透明模糊材质（预览浮窗 / 批注卡 / 页签条）、多层级柔和阴影、主题感知的悬停色罩（color-mix）与焦点内描边。
- 批注角标、面板计数、回收站徽标改为 Fluent 圆形徽标；批注卡 / 添加批注对话框 / 回收站对话框采用 Fluent 对话框阴影与入场动画。
- 页签栏改为 Fluent 填充式激活页签（细描边 + 微阴影），移除旧玻璃浮雕与硬编码暗色模式样式。
## v3.0.1 (2026-08-18)

### 修复
- 编辑批注窗口限高：内容特别多时编辑框不再无限变高，超出部分在卡片内滚动，操作按钮（删除/取消/保存）固定底部始终可见；卡片自动避让视口（上下越界均回移）。
- 预览浮窗文字改为正文大小（跟随 --orca-fontsize，兜底 15px），高度上限放宽适配。
- 编辑窗口（原文/输入框）字号改为正文大小。

### 改进
- 编辑批注窗口支持 Esc 键取消关闭。

## v3.0.0 (2026-08-18)

### 重构
- 插件升级为三合一「批注工具箱」：合并 orca-tab-bar（编辑器页签栏）与 orca-trash-bin（回收站）功能，三功能均在插件设置中可独立开关（页签栏 / 回收站默认开启）。
- 页签栏与回收站作为可选模块（`src/modules/`）接入，命名空间统一为 orca-pizhu，样式合并注入。

### 修复
- 批注后文字字号丢失：在放大字号的文字或标题文字上作批注时不再变回默认大小，批注时捕获原文真实样式（字号/颜色/粗斜体）并还原。
- 放大字号文字批注后角标位置异常：字号还原后角标随基线回到正确位置。
- 编辑批注时悬停预览失效，避免与编辑卡重叠干扰。

### 改进
- 去掉批注悬停的「批注，点击查看」浏览器原生 title 提示。
- 角标数字改为天蓝色。
- 编辑批注卡加宽（520px）且编辑框随文字内容自动伸缩高度。

## v0.2.0 (2026-08-18)

### 修复
- 批注后文字字号丢失：在放大字号的文字或标题文字上作批注时，文字不再变回默认大小。批注时捕获原文真实样式（字号/颜色/粗斜体），渲染时原样还原；捕获不到时不强制设置，保持块级继承。
- 放大字号文字批注后角标位置异常：字号还原后外层标记保持原字号，角标随基线回到正确位置。

## v0.1.0 (2026-08-14)

### 功能
- 选中文字添加批注，波浪线 + 角标数字标记（块内序号）
- 悬停预览批注内容，浮窗跟随鼠标并自动避开指针
- 点击批注打开批注卡，可编辑、删除；删除时还原原文格式
- 批注数据全部存储在内联 fragment 中，跨设备同步、复制粘贴天然正确
- 侧边面板汇总当前文档批注：序号、原文、批注内容、所在块预览；点击跳转，悬停删除
- 顶栏「批注」按钮打开面板，编辑器工具栏「添加批注」按钮
